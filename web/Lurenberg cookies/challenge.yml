# This file represents the base specification of your challenge. It is used by
# other tools to install and deploy your challenge.

# Required sections
name: "Lurenberg cookies"
author: "AzeTIx"
category: "Web"
description: "On a trouvé le blog personnel d'un membre de l'organisation, On sait qu'il cache des infos intéressantes dans un dossier /flag.txt
mais on n'a pas réussi à accéder au fichier, on nous a dit qu'il adore les cookies typiques de Lurenberg !




https://esaipctf:5000


**Author: AzeTIx**"
#TODO : change adress
value: 500
type: dynamic

# The extra field provides additional fields for data during the install/sync commands/
# Fields in extra can be used to supply additional information for other challenge types
# For example the follow extra field is for dynamic challenges. To use these following
# extra fields, set the type to "dynamic" and uncomment the "extra" section below
extra:
    initial: 500
    decay: 30
    minimum: 100

# Flags specify answers that your challenge use. You should generally provide at least one.
# Can be removed if unused
# Accepts strings or dictionaries of CTFd API data
flags:
    # A static case sensitive flag
    - ECTF{Why_4r3_w3_us1ng_cust0m_s4n1t1z4t10n?}

# Tags are used to provide additional public tagging to a challenge
# Can be removed if unused
# Accepts strings
tags:
    - difficile
    - web

files:
    - dist/Lurenberg_cookies.zip